{
  "name": "pupismyname",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean; npm run build:css; ENV=production npx @11ty/eleventy",
    "build:css": "node -e 'require(\"./lib/process-styles.js\")();'",
    "clean": "rm -rf _site",
    "dev": "npm run clean; npm run watch:css & ENV=development npx @11ty/eleventy --serve --incremental --quiet",
    "watch:css": "watch 'npm run build:css' ./styles"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-fetch": "^3.0.0",
    "@11ty/eleventy-navigation": "^0.3.5",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "cheerio": "^1.0.0-rc.12",
    "cssnano": "^5.1.15",
    "luxon": "^3.2.1",
    "normalize.css": "^8.0.1",
    "postcss": "^8.4.21",
    "postcss-import": "^15.1.0",
    "sharp": "^0.31.3",
    "watch": "^1.0.2"
  }
}
