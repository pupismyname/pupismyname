{
  "name": "pupismyname",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean; npx @11ty/eleventy",
    "clean": "rm -r _site",
    "dev": "npm run clean; npx @11ty/eleventy --serve --quiet --incremental",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2",
    "@11ty/eleventy-navigation": "^0.3.5",
    "@11ty/eleventy-plugin-rss": "^1.2.0",
    "luxon": "^2.5.0"
  }
}
