---
layout: layout-content.liquid
eleventyExcludeFromCollections: true
pagination:
  data: collections.categories
  size: 1
  alias: category
permalink: /{{ category.name }}/
eleventyComputed:
  title: "{{ category.plural }}"
---

{% assign list = collections[category.name] | reverse %}
{% include "list-all.liquid", list: list, listTags: true -%}
